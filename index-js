const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;
app.get('/', (req, res) => {
    res.send('Mukesh');
});
app.listen(PORT, () => {
    console.log(`Server is running on http://98.80.175.209:${PORT}`);
});
